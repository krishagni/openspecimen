export default {
  layout: {
    rows: [
      { fields: [ { name: "folder.name" } ] },

      { fields: [ { name: "folder.sharedWithAll" } ] },

      { fields: [ { name: "folder.allowEditsBySharedUsers" } ] },

      { fields: [ { name: "folder.sharedWith" } ] },

      { fields: [ { name: "folder.sharedWithGroups" } ] }
    ]
  }
}
