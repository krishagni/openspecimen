export default {
  layout: {
    rows: [
      { fields: [ { name: 'rateList.name' } ] },
      { fields: [ { name: 'rateList.description' } ] },
      { fields: [ { name: 'rateList.startDate' } ] },
      { fields: [ { name: 'rateList.endDate' } ] },
      { fields: [ { name: 'rateList.currency' } ] }
    ]
  }
}
